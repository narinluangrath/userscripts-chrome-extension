/**
 * The main script for the chrome extension
 * Attaches event handlers
 *
 * 1. Add tab.onCreated event handler. The handler should search
 *    for all UserScripts that match the tab's URL and execute them
 *
 */
